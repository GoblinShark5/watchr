/* eslint-disable linebreak-style */
import React, { useState } from 'react';

// Card that displays movie posters
const MoviePoster = (props) => {
    // We will need a tmdb api key to continue -Max
}

export default MovieCard;